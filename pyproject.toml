[project]
name = "sparklink-control-plane"
version = "0.1.0"
description = "Small auditable SparkLink production MVP control plane"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
